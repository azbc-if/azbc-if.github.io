function table_header(a, b, c, d, e, f) {

  xdump("<small>");
  xdump("<table class=\"table_header\"");
  xdump("<tr><th class=\"th_a\">");
  xdump(a);
  xdump("</th><th class=\"th_b\">");
  xdump(b);
  xdump("</th><th class=\"th_c\">");
  xdump(c);
  xdump("</th><th class=\"th_d\">");
  xdump(d);
  xdump("</th><th class=\"th_e\">");
  xdump(e);
  xdump("</th><th class=\"th_f\">");
  xdump(f);
  xdump("</th></tr></table>");
  xdump("</small>");

}

